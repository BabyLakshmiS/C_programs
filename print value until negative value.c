#include<stdio.h>
#include<stdlib.h>
int main()
{
    int i,num;
    int arr[100];
    for(i=0;i<100;i++)
    {
        scanf("%d",&arr[i]);
        if(arr[i]==(arr[i]-arr[i]-1))
        break;
    }
    for(i=0;i<arr[i];i++)
    {
        printf("%d ",arr[i]);
    }
}
